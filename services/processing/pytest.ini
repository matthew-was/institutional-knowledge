[pytest]
asyncio_mode = auto
markers =
    integration: Full integration tests requiring Ollama and Docling (local only)
    ci_integration: CI integration tests — no external services required
; addopts = --cov=. --cov-report=html --cov-report=term-missing

