{
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:backend": "pnpm --filter backend test",
    "lint": "pnpm -r biome check",
    "lint:backend": "pnpm exec biome check apps/backend/src"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.6",
    "typescript": "5.9.3"
  }
}
