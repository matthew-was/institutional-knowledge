{
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r biome check"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.6",
    "typescript": "5.9.3"
  }
}
